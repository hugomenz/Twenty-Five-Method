# Assets

This directory contains data assets only:

- `rhythm-pattern.schema.json`: persisted pattern contract.
- `rhythm-presets.json`: eight starter patterns for M25.

No font files are included. Bundle the selected VexFlow music and text fonts through the application dependency or the application's own asset pipeline.
